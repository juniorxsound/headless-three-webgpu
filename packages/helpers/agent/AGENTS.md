# render.gl GLTF helper agent instructions

Before using `@rendergl/headless-three-webgpu-helpers`, read:

- `../README.md`
- `./skills/rgl-gltf/SKILL.md`

Prefer installed package behavior over model memory. The files in this directory are versioned with the package in `node_modules`.
